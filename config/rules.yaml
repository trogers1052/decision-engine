# =============================================================================
# Decision Engine - Rule Configuration
# =============================================================================
#
# BACKTESTED STRATEGY (2021-01-01 to 2026-01-25)
# Symbols: SLV, CCJ, CAT, MP, PPLT, UUUU, URNM, ETN, AVAV, IAUM
# Results: 309 trades, 57% win rate, +117.4% avg return
#
# Trading Philosophy: "Buy quality dips with confirmation"
# - Enhanced entry filters (volume, trend spread, price support)
# - Multiple confirmation signals (RSI recovery + MACD)
# - Trend continuation on pullbacks to moving averages
#
# Exit Strategy:
# - Profit target: 7%
# - Stop loss: 5%
# - Min confidence: 0.5
#
# =============================================================================

service:
  name: decision-engine
  version: "2.0.0"

# Only evaluate symbols listed in active_tickers — the tradeable universe.
# Context/indicator symbols (SPY, QQQ, sector ETFs) flow through stock.indicators
# but should NOT be evaluated for trading signals — context-service handles them.
active_tickers_only: true

# Daily loss circuit breaker — halts all BUY signals when the account drops
# more than this percentage from the day's opening equity.  SELL signals still
# pass through.  Set to 0.05 = 5% ($50 on a $1,000 account = one month's
# contribution).  Tightened from 8% per portfolio risk audit.
daily_loss_threshold_pct: 0.05

# -----------------------------------------------------------------------------
# ENHANCED RULES (Backtested - Primary Strategy)
# -----------------------------------------------------------------------------
rules:
  # PRIMARY: Enhanced dip buying with strict filters
  enhanced_buy_dip:
    enabled: true
    description: "Buy RSI < 35, volume confirmed, trend spread > 1.5%, price > SMA_200"
    rsi_oversold: 35.0
    rsi_extreme: 30.0
    min_trend_spread: 1.5
    require_volume_confirm: true
    weight: 2.0  # Primary entry signal

  # MOMENTUM: Catch reversals with RSI + MACD confluence
  momentum_reversal:
    enabled: true
    description: "Buy when RSI recovering from oversold AND MACD crossing bullish"
    rsi_recovery_min: 30.0
    rsi_recovery_max: 45.0
    weight: 1.8

  # CONTINUATION: Buy pullbacks in strong trends
  trend_continuation:
    enabled: true
    description: "Buy when price pulls back to SMA_20 in strong uptrend"
    pullback_tolerance_pct: 2.0
    weight: 1.5

  # SCALE-IN: Removed — philosophically dangerous at $888 account size.
  # Guards were insufficient (RSI 35 too loose, 2% drop too small, no earnings blackout).
  # Revisit at $10k+ with Telegram confirmation gate and earnings calendar integration.
  average_down:
    enabled: false
    description: "Add to position when RSI < 35 and price 2%+ below entry"
    rsi_extreme: 35.0
    max_scale_ins: 2
    min_drop_from_entry_pct: 2.0
    weight: 1.2

# -----------------------------------------------------------------------------
# LEGACY RULES (Disabled - kept for reference)
# -----------------------------------------------------------------------------
  # Original buy dip rule (replaced by enhanced_buy_dip)
  buy_dip_in_uptrend:
    enabled: false
    description: "Buy when RSI < 40 AND weekly uptrend intact (SMA_20 > SMA_50)"
    rsi_threshold: 40.0
    weight: 1.5

  # Original strong buy (replaced by momentum_reversal)
  strong_buy_signal:
    enabled: false
    description: "Buy when RSI < 35 AND full trend alignment (SMA_20 > SMA_50 > SMA_200)"
    rsi_threshold: 35.0
    weight: 2.0

  # Weekly Uptrend Check
  weekly_uptrend:
    enabled: false
    description: "Stock trending up over past week"
    weight: 1.0

  # Monthly Uptrend Check
  monthly_uptrend:
    enabled: false
    description: "Stock trending up over past month"
    weight: 0.8

# -----------------------------------------------------------------------------
# Supporting Indicator Rules
# -----------------------------------------------------------------------------
  # RSI Rules
  rsi_oversold:
    enabled: true
    description: "Classic oversold condition"
    threshold: 30.0
    extreme_threshold: 20.0
    weight: 1.0

  rsi_overbought:
    enabled: true
    description: "Classic overbought - potential exit"
    threshold: 70.0
    extreme_threshold: 80.0
    weight: 1.0

  # MACD Rules
  macd_bullish_crossover:
    enabled: false  # Covered by momentum_reversal
    description: "MACD crosses above signal line"
    weight: 1.0

  macd_bearish_crossover:
    enabled: true
    description: "MACD crosses below signal line - exit warning"
    weight: 1.0

  # Trend Rules
  trend_alignment:
    enabled: true
    description: "Full bullish alignment: SMA_20 > SMA_50 > SMA_200"
    weight: 1.2

  trend_break_warning:
    enabled: true
    description: "SMA_20 crosses below SMA_50 - uptrend may be ending"
    weight: 1.0

# -----------------------------------------------------------------------------
# Mining Stock Specific Rules
# -----------------------------------------------------------------------------
  # Commodity Breakout - miners leverage commodity moves 2-3x
  commodity_breakout:
    enabled: true
    description: "Buy miners when breaking out 2%+ above SMA_20 with momentum"
    breakout_threshold_pct: 2.0
    min_trend_strength: 1.0
    weight: 1.5

  # Miner-Metal Ratio Mean Reversion
  miner_metal_ratio:
    enabled: true
    description: "Buy miners when oversold at support (underperforming metal)"
    rsi_oversold: 35.0
    support_tolerance_pct: 3.0
    weight: 1.6

  # Dollar Weakness = Miner Strength
  dollar_weakness:
    enabled: true
    description: "Buy miners in strong uptrend (indicates USD weakness)"
    min_trend_spread: 2.0
    require_macd_positive: true
    weight: 1.3

  # Seasonality Filter
  seasonality:
    enabled: true
    description: "Adjust confidence based on seasonal patterns (Jan-Feb, Aug-Sep strong)"
    strong_month_boost: 0.10
    weak_month_penalty: 0.15
    weight: 1.0

  # Volume Breakout - critical for mining stocks
  volume_breakout:
    enabled: true
    description: "Buy miners on 1.5x+ volume breakouts"
    min_volume_ratio: 1.5
    breakout_threshold_pct: 2.0
    weight: 1.8

# -----------------------------------------------------------------------------
# Signal Aggregation (Backtested Settings)
# -----------------------------------------------------------------------------
aggregation:
  method: "consensus_boost"  # Boost confidence when multiple rules agree
  min_confidence: 0.5        # Backtested threshold
  require_consensus: false   # Disabled — consensus_boost already rewards multi-rule agreement
  consensus_min_rules: 1     # Single-rule signals allowed; boost differentiates quality

# -----------------------------------------------------------------------------
# Exit Strategy (For Alert Service / Trading Journal)
# -----------------------------------------------------------------------------
exit_strategy:
  profit_target: 0.12  # 12% - best backtested performance
  stop_loss: 0.06      # 6% - best backtested performance
  # Risk/reward ratio: 2:1
  # Backtest results: 236 trades, 51.7% win rate, +157.1% avg return

# -----------------------------------------------------------------------------
# Ranking Configuration (Which stock to buy first?)
# -----------------------------------------------------------------------------
ranking:
  criteria: "composite"
  weights:
    dip_depth: 0.30      # Lower RSI = better entry
    trend_strength: 0.30  # Full SMA alignment preferred
    confidence: 0.25      # How many rules triggered
    volatility: 0.15      # Based on ATR

  # How often to publish rankings
  publish_interval_seconds: 60

  # Only rank if at least this many symbols have signals
  min_symbols_for_ranking: 2

# -----------------------------------------------------------------------------
# Output Configuration
# -----------------------------------------------------------------------------
output:
  # Publish individual symbol decisions
  publish_decisions: true
  decision_topic: "trading.decisions"

  # Publish cross-symbol rankings
  publish_rankings: true
  ranking_topic: "trading.rankings"

  # Debounce to avoid flooding (seconds)
  debounce_seconds: 5

  # Only publish signals above this confidence
  min_publish_confidence: 0.5

# -----------------------------------------------------------------------------
# Active Tickers (Backtested 2021-01-01 to 2026-01-25)
# -----------------------------------------------------------------------------
# ONLY trade these symbols - they have proven backtested results.
# Removed: ETN (45.8% WR), HL (-47% DD), PAAS (0.17 Sharpe), FCX (0.18 Sharpe)
# -----------------------------------------------------------------------------
active_tickers:

  # =========================================================================
  # TIER 1 - BEST PERFORMERS (Sharpe > 0.75)
  # Backtested 2021-01-01 to 2026-02-22, daily signals + 5min exits
  # =========================================================================

  AEM:
    # VALIDATED 2026-03-08: 3/4 gates passed (WB+BS+MC, Regime fails — gold miner naturally bull-concentrated)
    # Walk-Backward ROBUST (2/3 regimes pass): 2022 Bear +2.0%, 2023-2024 Chop +28.6%
    # Bootstrap p=0.0003 (strongest edge signal), MC Ruin=0%, P95 DD=-19.2%
    # Regime fails at >70% bull concentration — expected for gold miner (not a disqualifier)
    # 4-rule lean mining set: momentum_reversal + commodity_breakout + seasonality + death_cross
    description: "Agnico Eagle Mines - Gold (78.3% WR, +171.9%, 1.40 Sharpe, 4.72 PF, -15.6% DD)"
    rules:
      - momentum_reversal
      - commodity_breakout
      - seasonality
      - death_cross
    exit_strategy:
      profit_target: 0.07
      stop_loss: 0.08
      max_loss_pct: 8.0
      cooldown_bars: 10
      win_rate: 0.783
      trades_per_year: 4
    min_confidence: 0.4

  CCJ:
    description: "Cameco - Uranium (63.3% WR, +161.2%, 0.99 Sharpe, 2.97 PF)"
    rules:
      - trend_continuation
      - seasonality
      - death_cross
    exit_strategy:
      profit_target: 0.10
      stop_loss: 0.06
      max_loss_pct: 6.0
      cooldown_bars: 7
      win_rate: 0.633
      trades_per_year: 8
    min_confidence: 0.65

  CAT:
    # VALIDATED 2026-03-02: 3/4 gates passed (WF+BS+MC, Regime borderline at 79% bull)
    # Profitable in bull+bear+volatile — only chop is negative (22% WR)
    # No restrictions needed — strong like CCJ/IAUM
    description: "Caterpillar - Industrial (53.2% WR, +147.3%, 1.52 Sharpe, 2.73 PF, -17.6% DD)"
    rules:
      - enhanced_buy_dip
      - momentum_reversal
      - trend_continuation
      - rsi_oversold
      - macd_bearish_crossover
      - trend_alignment
      - trend_break_warning
      - death_cross
      - seasonality
    exit_strategy:
      profit_target: 0.12
      stop_loss: 0.04
      max_loss_pct: 4.0
      cooldown_bars: 3
      win_rate: 0.532
      trades_per_year: 16
    min_confidence: 0.5

  WPM:
    # VALIDATED 2026-03-02: 3/4 gates passed (WF+BS+MC, Regime borderline at 79% bull)
    # WF ratio 289% — strategy improves in out-of-sample (gold rally 2024-2026)
    # Profitable or flat in ALL regimes — no restrictions needed
    description: "Wheaton Precious Metals - Streamer (45.9% WR, +170.7%, 0.74 Sharpe, 2.14 PF, -30.7% DD)"
    rules:
      - enhanced_buy_dip
      - momentum_reversal
      - trend_continuation
      - rsi_oversold
      - macd_bearish_crossover
      - trend_alignment
      - golden_cross
      - trend_break_warning
      - death_cross
      - seasonality
    exit_strategy:
      profit_target: 0.12
      stop_loss: 0.04
      max_loss_pct: 4.0
      cooldown_bars: 3
      win_rate: 0.459
      trades_per_year: 15
    min_confidence: 0.5

  PPLT:
    # VALIDATED 2026-03-01: 4/4 gates passed (WF+BS+MC+Regime)
    # Only symbol to pass all 4 validation gates
    # Key change: +commodity_breakout diversifies regime exposure (73%→60% bull)
    description: "Platinum ETF - The Compounder (61.5% WR, +89.1%, 1.00 Sharpe, 2.20 PF, -14.8% DD)"
    rules:
      - enhanced_buy_dip
      - momentum_reversal
      - trend_continuation
      - trend_alignment
      - golden_cross
      - trend_break_warning
      - death_cross
      - commodity_breakout
    exit_strategy:
      profit_target: 0.06
      stop_loss: 0.04
      max_loss_pct: 4.0
      cooldown_bars: 3
      win_rate: 0.615
      trades_per_year: 13
    min_confidence: 0.65

  OKE:
    # VALIDATED 2026-03-08: 4/4 gates passed (WB+BS+MC+Regime) — best energy stock
    # Walk-Backward ROBUST (2/3 regimes pass): 2022 Bear +3.5%, 2023-2024 Chop +31.9%
    # Bootstrap p=0.0194 (statistically significant), MC Ruin=0%, P95 DD=-19.0%
    # Regime: well distributed — bull:9t/+28.1%, bear:2t/+20.9%, chop:1t/+11.7%
    # Energy combo: energy_momentum + dip_recovery + energy_seasonality + death_cross
    description: "ONEOK - Midstream Energy (75% WR, +65.5%, 1.09 Sharpe, 3.48 PF, -14.7% DD)"
    rules:
      - energy_momentum
      - dip_recovery
      - energy_seasonality
      - death_cross
    exit_strategy:
      profit_target: 0.10
      stop_loss: 0.08
      max_loss_pct: 8.0
      cooldown_bars: 5
      win_rate: 0.75
      trades_per_year: 3
    min_confidence: 0.6

  PSX:
    # VALIDATED 2026-03-08: 3/4 gates passed (WB+MC+Regime, Bootstrap fails p=0.0344)
    # Walk-Backward ROBUST (2/3 regimes pass): 2022 Bear +6.9%, 2023-2024 Chop +27.9%
    # Bootstrap p=0.0344 (very close to 0.05 threshold), MC Ruin=0%, P95 DD=-26.0%
    # Regime: well distributed — bull:20t/+38.4%, bear:2t/+5.6%, chop:1t/+10.4%
    # Highest Sharpe in energy cohort (1.53)
    # Energy combo: energy_momentum + energy_seasonality + death_cross
    description: "Phillips 66 - Integrated Energy (58% WR, +62.9%, 1.53 Sharpe, 1.89 PF, -20.8% DD)"
    rules:
      - energy_momentum
      - energy_seasonality
      - death_cross
    exit_strategy:
      profit_target: 0.08
      stop_loss: 0.05
      max_loss_pct: 5.0
      cooldown_bars: 5
      win_rate: 0.58
      trades_per_year: 6
    min_confidence: 0.5

  # =========================================================================
  # TIER 2 - GOOD PERFORMERS (Sharpe 0.5-0.75)
  # =========================================================================

  IAUM:
    # VALIDATED 2026-03-02: 3/4 gates passed (WF+BS+MC, Regime fails at 83% bull)
    # Strongest p-value in portfolio (0.0002), safest P95 DD (-15.3%)
    # No config change needed — baseline is already optimal
    description: "iShares Gold Micro (61.5% WR, +96.6%, 0.80 Sharpe, 2.80 PF, -11.8% DD)"
    rules:
      - enhanced_buy_dip
      - momentum_reversal
      - trend_continuation
      - rsi_oversold
      - macd_bearish_crossover
      - trend_alignment
      - golden_cross
      - trend_break_warning
      - death_cross
      - seasonality
    exit_strategy:
      profit_target: 0.05
      stop_loss: 0.04
      max_loss_pct: 4.0
      cooldown_bars: 3
      win_rate: 0.615
      trades_per_year: 8
    min_confidence: 0.5

  URNM:
    # VALIDATED 2026-03-02: 3/4 gates passed (WF+BS+MC, Regime fails — uranium secular bull)
    # Strongest bootstrap in portfolio: p=0.0052, Sharpe CI [0.72, 5.58]
    # Key change: added commodity_breakout (+30% Sharpe, +41% return, less DD)
    # Outperforms sister ticker CCJ in Sharpe (0.90 vs 0.83) and PF (2.74 vs 2.04)
    description: "Uranium Mining ETF (43.8% WR, +123.3%, 0.90 Sharpe, 2.74 PF, -17.3% DD)"
    rules:
      - trend_continuation
      - seasonality
      - death_cross
      - commodity_breakout
    exit_strategy:
      profit_target: 0.12
      stop_loss: 0.04
      max_loss_pct: 4.0
      cooldown_bars: 7
      win_rate: 0.438
      trades_per_year: 6
    min_confidence: 0.65

  SLV:
    # VALIDATED 2026-03-01: 2/4 gates passed (Bootstrap + Monte Carlo)
    # Fails Walk-Forward (negative train Sharpe) and Regime (87% bull-dependent)
    # Conditional deployment: BULL/CHOP regimes only, half position size
    description: "Silver ETF (48.6% WR, +77.1%, 0.53 Sharpe, 2.08 PF, -16.8% DD)"
    rules:
      - enhanced_buy_dip
      - momentum_reversal
      - trend_continuation
      - rsi_oversold
      - rsi_overbought
      - macd_bearish_crossover
      - trend_alignment
      - trend_break_warning
      - commodity_breakout
      - miner_metal_ratio
      - dollar_weakness
      - seasonality
      - volume_breakout
      - death_cross
    exit_strategy:
      profit_target: 0.12
      stop_loss: 0.04
      max_loss_pct: 4.0
      cooldown_bars: 7
      win_rate: 0.486
      trades_per_year: 14
    min_confidence: 0.55
    allowed_regimes:            # Only trade in these regimes (validated 2026-03-01)
      - BULL
      - CHOP

  # =========================================================================
  # TIER 3 - HIGH RISK / HIGH REWARD (Use smaller position sizes)
  # =========================================================================

  RTX:
    # VALIDATED 2026-03-02: 3/4 gates passed (WF+BS+MC, Regime fails)
    # Multi-TF critical — unlocks Bootstrap (p=0.0555 daily → p=0.0212 multi-TF)
    # WF ratio 497% — strongest walk-forward in portfolio
    # Lean 3-rule set works (like commodity miners). PF=3.29, DD=-15.9%.
    # Half position sizing until trade count reaches 20+ (only 11 trades)
    description: "RTX Corp - Defense (54.5% WR, +55.4%, 0.55 Sharpe, 3.29 PF, -15.9% DD)"
    rules:
      - trend_continuation
      - seasonality
      - death_cross
    exit_strategy:
      profit_target: 0.12
      stop_loss: 0.05
      max_loss_pct: 5.0
      cooldown_bars: 5
      win_rate: 0.545
      trades_per_year: 2
    min_confidence: 0.65

  ITA:
    # VALIDATED 2026-03-02: 3/4 gates passed (WF+MC+Regime, Bootstrap marginal p=0.0814)
    # One of only 4 symbols to pass Regime — bull contributes only 39% of profit
    # Defense is counter-cyclical: bear +11.4%, crisis +10.3%
    # Full 10-rule set works (unlike commodity miners). Daily-only exits (multi-TF degrades).
    # Half position sizing until bootstrap edge confirmed with more trades
    description: "iShares US Aerospace & Defense ETF (61.5% WR, +37.4%, 0.52 Sharpe, 2.17 PF, -22.5% DD)"
    rules:
      - enhanced_buy_dip
      - momentum_reversal
      - trend_continuation
      - rsi_oversold
      - macd_bearish_crossover
      - trend_alignment
      - golden_cross
      - trend_break_warning
      - death_cross
      - seasonality
    exit_strategy:
      profit_target: 0.10
      stop_loss: 0.05
      max_loss_pct: 5.0
      cooldown_bars: 3
      win_rate: 0.615
      trades_per_year: 3
    min_confidence: 0.50

  MP:
    # VALIDATED 2026-03-02: 3/4 gates passed (WF+MC+Regime, Bootstrap marginal at p=0.0414)
    # Only symbol besides PPLT to pass Regime gate — profit well distributed across regimes
    # Key change: PT 12%→15% (13x better return). Lean 3-rule set works best.
    # Conditional: half position sizing until bootstrap edge confirmed with more trades
    description: "MP Materials - Rare Earth (60.5% WR, +70.8%, 0.39 Sharpe, 1.76 PF, -26.1% DD)"
    rules:
      - trend_continuation
      - seasonality
      - death_cross
    exit_strategy:
      profit_target: 0.15
      stop_loss: 0.05
      max_loss_pct: 5.0
      cooldown_bars: 5
      win_rate: 0.605
      trades_per_year: 8
    min_confidence: 0.5

  UUUU:
    # VALIDATED 2026-03-01: 2/4 gates passed (Bootstrap + Monte Carlo)
    # Fails Walk-Forward (negative train Sharpe) and Regime (bull-dependent)
    # Conditional deployment: BULL/CHOP regimes only, half position size
    description: "Energy Fuels - Uranium (49.5% WR, +134.4%, 0.40 Sharpe, 1.97 PF, -43.4% DD)"
    rules:
      - enhanced_buy_dip
      - momentum_reversal
      - trend_continuation
      - rsi_oversold
      - macd_bearish_crossover
      - trend_alignment
      - trend_break_warning
      - death_cross
      - seasonality
    exit_strategy:
      profit_target: 0.20
      stop_loss: 0.04
      max_loss_pct: 4.0
      cooldown_bars: 3
      win_rate: 0.495
      trades_per_year: 6
    min_confidence: 0.55
    allowed_regimes:            # Only trade in these regimes (validated 2026-03-01)
      - BULL
      - CHOP

# -----------------------------------------------------------------------------
# DO NOT TRADE - Removed due to poor backtested performance:
# -----------------------------------------------------------------------------
# ETN:  45.8% WR, below 50% = losing strategy
# HL:   CONFIRMED BLACKLIST — validated 2026-02-28, 0-1/4 gates passed
#       36.8% WR, -0.41 train Sharpe, p=0.22, P95 DD=-52%
#       No statistical edge exists (5 baselines + 13 tune configs tested)
# LMT:  VALIDATED 2026-03-02: 2/4 gates max (MC+Regime only)
#       Negative train Sharpe across ALL configs. Best: +4.6%, 0.00 Sharpe. No edge.
# AVAV: VALIDATED 2026-03-02: 2/4 gates max (WF+Regime only)
#       MC fails (P95 DD=-43.5%). High return (+104%) but too risky.
# NEP:  BLACKLISTED 2026-03-08 — structural decline (-85% total return), untradeable for long-only.
#       No rules produce edge. Renewables yieldco in terminal distribution cut spiral. Data purged from TimescaleDB.
# PAAS: 48.1% WR, +14.2%, 0.17 Sharpe, -55.9% DD = not worth it
# FCX:  52.9% WR, +15.9%, 0.18 Sharpe, -39.5% DD = not worth it
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Trade Plan Engine — deterministic entry/stop/target/R:R generation
# Every BUY decision is enriched with a complete trade plan before publishing.
# $0 cost — no LLM involved.
# -----------------------------------------------------------------------------
trade_plan_engine:
  enabled: true
  atr_multiplier: 2.0           # Stop = entry - (ATR_14 × atr_multiplier)
  min_rr_ratio: 2.0             # Plans with R:R below this set plan_valid=false + rr_warning
  stop_min_pct: 3.0             # Widen ATR stop if % distance < this (to 4%)
  stop_max_pct: 15.0            # Cap ATR stop if % distance > this (to 10%)
  default_account_balance: 1000.00  # Fallback when Redis is unavailable
  account_balance_redis_key: "robinhood:portfolio"  # Redis hash → total_equity field
  validity_windows:
    oversold_bounce: "eod"       # Valid until end of trading day
    pullback_to_support: "2d"    # Valid for 2 trading days
    breakout: "2h"               # Valid for 2 hours (breakouts go or fail fast)
    signal: "eod"                # Generic signals valid until EOD

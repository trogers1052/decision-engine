# Decision Engine Dependencies (pinned to production versions)

# Kafka
kafka-python==2.3.0

# Configuration
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.2.2
PyYAML==6.0.3

# Redis (for state persistence)
redis==7.3.0

# Database (optional, for decision logging)
psycopg2-binary==2.9.11

# Risk Engine (position sizing, portfolio risk)
# Installed from local source in Dockerfile (COPY risk-engine/)
# For local dev: pip install -e ../risk-engine/

# Metrics
prometheus-client==0.25.0

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0

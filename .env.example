# Decision Engine Configuration
# Copy to .env and fill in values

# Kafka Configuration
KAFKA_BROKERS=localhost:19092
KAFKA_CONSUMER_GROUP=decision-engine
KAFKA_INPUT_TOPIC=stock.indicators
KAFKA_DECISION_TOPIC=trading.decisions
KAFKA_RANKING_TOPIC=trading.rankings

# Redis Configuration (for state persistence)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=1

# Database Configuration (for decision logging)
DB_HOST=localhost
DB_PORT=5432
DB_USER=trader
DB_PASSWORD=trader5
DB_NAME=trading_platform

# Rule Configuration
RULES_CONFIG_PATH=config/rules.yaml

# Processing Settings
MIN_PUBLISH_CONFIDENCE=0.5
RANKING_INTERVAL_SECONDS=60
DEBOUNCE_SECONDS=5
